[package]
name = "imagegen"
version = "0.3.0"
description = "Generate and edit images with the Gemini image model as Katari tools. Pure Katari — no sidecar."

[runtime]
url = "http://localhost:3000"

[dependencies]
registry = "https://raw.githubusercontent.com/katari-lang/katari-registry/main"
snapshot = "staging"
packages = []
