[package]
name = "imagegen"
version = "0.1.1"
description = "Edit images with the Gemini image model as a Katari tool. Pure Katari — no sidecar."

[runtime]
url = "http://localhost:3000"

[dependencies]
registry = "https://raw.githubusercontent.com/katari-lang/katari-registry/main"
snapshot = "staging"
packages = []
